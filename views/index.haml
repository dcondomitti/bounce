%h2 
	Load Balancers
	- @balancers.each do |balancer|
		%h3
			= balancer.name
		%p
			%strong
				Cluster Name
			= balancer['normal']['elasticsearch']['cluster_name']
			%a{:href => elasticsearch_head_url(balancer.name) } elasticsearch-head
			%a{:href => bigdesk_url(balancer.name) } bigdesk

%h2
	Individual Nodes
	- @nodes.each do |node|
		%h3
			= node.name
		%p
			%strong
				Cluster Name
			= node['normal']['elasticsearch']['cluster_name']
			%a{:href => elasticsearch_head_url(node.name) } elasticsearch-head
			%a{:href => bigdesk_url(node.name) } bigdesk